# test_global_job
#
# A test global job for demonstration
#
# Generated by DeepWork - do not edit manually

description = "A test global job for demonstration"

prompt = """
# test_global_job

**Multi-step workflow**: A test global job for demonstration

> **NOTE**: Gemini CLI requires manual command invocation. After each step, tell the user which command to run next.

This is a test job to demonstrate global job functionality

## Available Steps

1. **test_step** - A simple test step
   Command: `/test_global_job:test_step`

## Execution Instructions

### Step 1: Analyze Intent

Parse any text following `/test_global_job` to determine user intent:
- "test_step" or related terms → start at `/test_global_job:test_step`

### Step 2: Direct User to Starting Step

Tell the user which command to run:
```
/test_global_job:test_step
```

### Step 3: Guide Through Workflow

After each step completes, tell the user the next command to run until workflow is complete.

### Handling Ambiguous Intent

If user intent is unclear:
- Present available steps as numbered options
- Ask user to select the starting point

## Reference

- Job definition: `.deepwork/jobs/test_global_job/job.yml`
"""